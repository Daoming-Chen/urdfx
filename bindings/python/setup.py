
from setuptools import setup

# This setup.py is a shim for older pip versions or tools that don't support pyproject.toml fully.
# It relies on the build backend specified in pyproject.toml.

setup()

